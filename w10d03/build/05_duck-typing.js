"use strict";
// if it looks like a duck, and walks like a duck, and quacks like a duck, then it's probably a duck
const login = (user) => {
    return true;
};
const user = {
    username: 'jstamos',
    password: '1234',
    moreStuff: 'hello'
};
login(user);
